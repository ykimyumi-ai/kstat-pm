'use strict';
/**
 * 2026 납품대금 연동제 실태조사 제안서 — 발췌 21장 원문
 *
 * 원본 PDF·이미지는 텍스트 레이어가 없는 래스터라, 전 장을 육안 판독해 한 자도
 * 빠짐없이 전사한 결과다. 문구·수치를 레이아웃 코드와 분리해 두었으므로 값이
 * 바뀌면 데이터만 고치고 `node build.js` 로 다시 출력하면 된다.
 *
 * 실제 데이터는 `content.json` 에 있다. 웹 편집기(`web/`)가 원문을 무손실로 읽고
 * 쓰려면 JSON 이어야 해서 데이터만 분리했고, 이 파일은 로더로 남는다.
 * CLI(`build.js`·`verify.py`)는 지금까지처럼 `require('./content')` 로 쓰면 된다.
 *
 * ── 스키마 메모 ────────────────────────────────────────────────────────────
 *   quote 의 runs   hi:true 는 파랑 볼드 강조, br:true 는 그 조각 뒤에서 줄바꿈.
 *                   본문 안의 강조 조각(18·20장의 what/use/supRuns 등)도 같은 규칙.
 *   tone            NAVY / GOLD / DARK — 번호 배지와 제목 색을 고른다.
 *   big · unit      원본에서 숫자만 크게 찍힌 줄. 숫자가 big, 뒤따르는 단위가 unit.
 *   tint (20장)     상단 3단 띠의 배경색 계열. big 은 굵게 키우는 꼬리 문구.
 *   ink1·ink2·inkU  17장 전용. 원본에서 잰 글자 잉크 높이(px)로, 노드마다 22~41px
 *   y1·y2           로 크게 달라 균일 적용하면 06·07번 노드 글자가 겹친다.
 *   img             그 장의 원본 좌표 공간(px). 슬라이드 코드의 좌표 단위가 된다.
 *   fixes           원본 오탈자를 교정한 내역. 되돌리려면 이 값을 참고한다.
 *
 *   21장(s21)만 img 가 3505×2480 이다 — 원본이 한글에서 뽑은 A4 가로 PDF(텍스트
 *   레이어 없음)라 300dpi 로 래스터화해 실측했기 때문이다.
 */

module.exports = require('./content.json');
